# Vibcoding 页面 — 执行步骤

> 给 v4pro 按顺序执行，完成一项再下一项

---

## 第 1 步：OpenCode 桌面版 — 改数据文件

打开 `D:\MyCode\vibcoding\data.js`，把它改成 `data.json`
1. 删掉 `const githubProjects = [` 这些变量声明，只保留纯 JSON
2. 所有 Viral 数据删掉 `video` 字段，改用 `watchUrl` 放链接
3. 去掉所有 YouTube 链接

---

## 第 2 步：OpenCode 桌面版 — 改 HTML + JS

1. 新建 `script.js`，用 `fetch('data.json')` 加载数据，把 index.html 底部渲染卡片的 JS 代码搬过来
2. 改 `index.html`：删掉 `<script src="data.js">` 改成 `<script src="script.js">`
3. 删掉所有 iframe 视频嵌入代码
4. 删掉所有 YouTube 相关代码
5. Viral 卡片只保留链接按钮：有 `watchUrl` 就显示 "▶️ 观看视频"

---

## 第 3 步：OpenCode 桌面版 — 改样式

打开 `style.css`，删掉视频相关的样式（`.video-wrapper`、`.video-container`、`.youtube-link` 等）

---

## 第 4 步：VS Code — 预览效果

双击 `D:\MyCode\vibcoding\index.html`，看看页面正不正常

---

## 第 5 步：Claude Code 终端（v4pro）— 检查文件结构

检查 `D:\MyCode\vibcoding` 目录，确认：
- 有没有 `data.json`？
- 有没有 `script.js`？
- data.json 里还有没有 `video` 字段和 YouTube 链接？
- index.html 还有没有 iframe？

---

## 第 6 步：找 Claude Code（v4lash）最终检查

切到当前对话，说：**"检查一下 vibcoding 页面"**

---

## 第 7 步：OpenCode 终端版 — 修问题

如果查出问题，把问题清单发给它修

---

## 第 8 步：Claude Code 终端或 VS Code 终端 — 推送

```bash
git add .
git commit -m "vibcoding 页面完成"
git push
```

---

## 第 9 步：手动 — 开 GitHub Pages

浏览器打开 GitHub 仓库 → Settings → Pages → 选 main 分支 → Save
