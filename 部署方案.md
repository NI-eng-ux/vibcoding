# Vibcoding 页面 — 部署到 GitHub Pages 完整方案

> 项目路径：`D:\MyCode\vibcoding`
> 给 v4pro 按顺序执行

---

## 分工

| 任务 | 谁做 |
|------|------|
| 修 deploy.yml | OpenCode 桌面版 |
| GitHub 建仓库 + 推送 | VS Code 终端 |
| 开 GitHub Pages | 手动（浏览器）|
| 验证能不能访问 | 手动（浏览器）|

---

## 第 1 步：修 deploy.yml（OpenCode 桌面版）

打开 `.github/workflows/deploy.yml`，把第 15 行的 `'data.js'` 改成 `'data.json'`，再加个 `'script.js'`：

**改前：**
```yaml
      - 'data.js'
```

**改后：**
```yaml
      - 'data.json'
      - 'script.js'
```

这样每次 data.json 或 script.js 有变动时，GitHub Pages 会自动重新部署。

---

## 第 2 步：GitHub 建仓库（VS Code 终端）

在 VS Code 终端依次执行：

```bash
cd D:\MyCode\vibcoding

# 看看当前状态
git status

# 把所有文件加进来
git add .
git commit -m "🎉 vibcoding 页面完成，准备部署 GitHub Pages"

# 添加远程仓库（换成你自己的 GitHub 用户名）
git remote add origin https://github.com/你的GitHub用户名/vibcoding.git

# 推送到 GitHub
git push -u origin main
```

> ⚠️ 如果 `git remote add origin` 那步提示 "already exists"，先运行 `git remote remove origin` 再重新添加

---

## 第 3 步：开 GitHub Pages（手动）

浏览器打开 GitHub 仓库：
```
https://github.com/你的GitHub用户名/vibcoding
```

点 **Settings** → 左侧 **Pages** → 在 **Branch** 那里选 `main` → `/ (root)` → **Save**

等 1-2 分钟。

---

## 第 4 步：验证（手动）

浏览器打开：
```
https://你的GitHub用户名.github.io/vibcoding/
```

看看页面能不能正常显示，卡片有没有出来。

---

## 搞定之后

告诉 Claude Code（v4lash）一声，我来做最终检查 ✅
