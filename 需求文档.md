# Vibcoding 热门作品分析 — 需求文档

> 最后更新：2026-07-24
> 交给 v4pro 直接开干用

---

## 一、项目概述

一个展示 **Vibcoding（氛围编程）热门作品** 的静态信息页面，数据由 AI 搜集整理，页面纯前端展示。

| 项目 | 内容 |
|------|------|
| **用途** | **给自己看，找灵感** — 每次打开能看到别人用 vibe coding 做了什么，激发自己下一个项目想法 |
| **风格** | 简洁现代 |
| **语言** | 中文 |
| **形式** | 单页面，纯 HTML + CSS + JS |

**核心目标：** 不是做一个"介绍页"，而是做一个**灵感板（inspiration board）**。用户打开能快速扫到有趣的项目，觉得"这个我也可以试试"或"原来还能这样玩"。

---

## 二、页面结构 (从上到下)

### 1. 顶部导航栏
- 左侧：Logo + 网站名称（"Vibcoding 灵感" 或 "Vibcoding 热门作品"）
- 右侧：小字显示数据更新时间
- **导航栏固定置顶**，滚动时保持可见

### 2. Hero 区域（大标题区）
- 主标题：吸引眼球的一句话，比如 *"别人用 vibe coding 做了什么？"*
- 副标题：简短解释 vibe coding（一两句话，不要长篇大论）
- 整体简洁、有冲击力

### 3. GitHub 热门项目区 — **灵感卡片**
展示格式：卡片网格（2~3 列，手机变 1 列）

每张卡片包含：
- **项目名称**（可点击跳转 GitHub）
- **一个醒目的"查看项目"按钮**（放在卡片底部，不能让用户找不到链接在哪）
- **Star 数量** + 小星星图标
- **一句话简介**
- **主要技术栈标签**（如 Python, JS, C++ 等）
- **💡 灵感点** — 这个项目最值得借鉴的地方（一句点明，让人看了想试）
- **分类标签**（如：工具类 / 游戏类 / 应用类 / 实验性）

### 4. Viral 作品案例区
展示格式：卡片网格

每张卡片包含：
- **作品名称**
- **作者/来源**
- **为什么火** — 这个作品 viral 的原因
- **💡 灵感点** — 从这里能学到什么 vibe coding 思路
- **外部链接**

### 5. 灵感随机区（亮点功能 ⭐）
- 一个大卡片或按钮：**"随手翻一个"**
- 点击后随机展示一个项目/作品
- 适合想不到做什么的时候，点一下碰运气

### 6. 技术栈速览
- 所有项目用到的技术栈汇总
- 简单展示哪些工具/语言出现最多
- 让用户一眼看出"大家都在用什么做"

### 7. 页脚
- "数据由 AI 搜集 · 持续更新"
- 更新日期

---

## 三、视觉风格

| 项目 | 要求 |
|------|------|
| 风格 | **简洁现代，偏杂志感**，像在看一份精选集 |
| 主色调 | **深色模式**（深灰/深蓝底），亮色点缀 |
| 字体 | 系统无衬线字体 |
| 卡片 | 圆角、柔和发光边框或阴影、悬停微微上浮 |
| 响应式 | 电脑多列，手机单列 |
| 图标 | Font Awesome 或 Lucide（CDN） |

**配色参考方向：**
- 背景：`#0f0f0f` 或 `#1a1a2e`
- 卡片：`#1e1e2e` 或 `#2a2a3e`
- 强调色：亮蓝 `#4fc3f7` 或 紫色 `#b388ff`
- 文字：浅灰白 `#e0e0e0`

---

## 四、数据内容（AI 负责搜集并填入）

### GitHub 热门项目
搜集方向：
- GitHub 上 vibe coding / AI-assisted programming 高星项目
- "vibe coding" 话题下的热门仓库
- 用 AI 辅助做出来的有趣项目

每条数据结构：
```js
{
  name: "项目名",
  stars: "12.3k",
  description: "一句话简介",
  tags: ["Python", "AI"],
  category: "工具类",     // 工具类 / 游戏类 / 应用类 / 实验性
  inspiration: "💡 这里写这个项目最值得借鉴的灵感点",
  url: "https://github.com/xxx"
}
```

### Viral 作品案例
搜集方向：
- Twitter / Reddit / 新闻上火过的 vibe coding 作品
- 用 AI 编程做出来的知名项目、有趣 prototype
- 有话题性的 AI 编程作品

**视频嵌入规则：**
- **只用 抖音**，不用 YouTube，不用 B站（用户测试发现 YouTube 播放不了）
- 抖音视频用嵌入格式：`https://www.douyin.com/video/视频ID`
- **必须使用真实有效的视频 ID**，不要随便填，否则页面会显示无法播放
- **没有视频的作品不放嵌入**，改为在卡片底部加一个醒目的 **"▶️ 观看视频" 链接按钮**，点击跳转到外部观看
- 所有 Viral 卡片默认都应该有一个视频链接（嵌入或跳转），不要留空

每条数据结构：
```js
{
  name: "作品名",
  author: "作者/来源",
  description: "为什么火",
  inspiration: "💡 从这里能获得的灵感",
  url: "链接",
  video: "https://www.douyin.com/video/xxxx"  // 有抖音视频就填，没有就填一个外部跳转链接
}
```

---

## 附：当前页面已知问题（v4pro 接手后需要修复）

> 当前已有一个初版页面，存在以下问题，需要在最终交付中修复：

| # | 问题类型 | 具体问题 | 修复要求 |
|--|---------|---------|---------|
| 1 | 🔴 功能缺陷 | **视频全是错的** — YouTube 播放不了，B站是假链接且本地 file:// 协议打不开 | 换成抖音嵌入，没有视频的加"▶️ 观看视频"跳转按钮 |
| 2 | 🔴 功能缺陷 | **GitHub 项目卡片没有明显的"查看项目"按钮**，只有项目名可以点，用户不知道能跳转 | 卡片底部加一个醒目的"查看项目"或"GitHub →"按钮 |
| 3 | 🟡 数据格式 | **数据文件是 `data.js` 而不是 `data.json`**，GitHub Actions 无法自动更新 | 改为 `data.json`，页面通过 fetch 加载 |
| 4 | 🟡 功能缺失 | **没有分类标签筛选**，数据缺少 `category` 字段 | 按要求补充分类标签 |
| 5 | 🟡 功能缺失 | **没有 GitHub Actions 自动更新配置** | 按第5节要求配置 |
| 6 | 🟡 功能缺失 | **没有推到 GitHub Pages** | 按第6节要求部署 |

---

## 五、数据更新机制

**更新频率：** 每 2 天自动更新一次

### GitHub 热门项目 → 全自动更新
- 使用 **GitHub Actions** 定时触发（cron: `0 0 */2 * *`）
- Action 运行 Node.js 脚本，通过 **GitHub Search API** 自动搜索以下关键词：
  - `"vibe coding"`、`"vibecoding"`、`"AI assisted programming"`
  - 按 star 数排序，取 top 项目
- 自动生成 `data.json` 文件
- 自动 commit + push 到仓库
- **不需要人工干预**

### Viral 作品案例 → v4pro 搜集填充
- 这部分没有标准化 API，由 v4pro 搜索整理填充到 `data.json`
- 一次性填充初始数据，后续如需更新再让 v4pro 重新搜

---

## 六、部署与维护（全部由 v4pro 完成）

### 6.1 推送到 GitHub
- 在 GitHub 创建公开仓库（名称：`vibcoding`）
- 将代码推送到 GitHub

### 6.2 启用 GitHub Pages
- 在仓库 Settings → Pages 中启用：
  - Source: **Deploy from a branch**
  - Branch: `main`
- 访问地址：`https://<用户名>.github.io/vibcoding/`

### 6.3 GitHub Actions 配置
- 确认仓库的 Actions 已启用（默认开启）
- **不需要额外添加 Secrets** — GitHub Search API 使用默认的 `GITHUB_TOKEN` 即可，Actions 运行时会自动注入
- 首次部署后，Actions 每 2 天自动运行一次，更新 GitHub 项目数据

### 6.4 后续维护
- 用户不需要做任何操作
- 每次打开页面都是最新的数据

---

## 七、技术要求

- 纯前端，不需要后端、不需要数据库
- 数据从 `data.json` 加载（不硬编码在 JS 中，方便 Actions 自动更新）
- 不依赖构建工具（webpack/vite 等）
- 直接用 CDN 加载外部库
- 在 Windows 下 Chrome/Edge 正常显示
- 不需要登录、评论、搜索功能

---

## 八、文件结构

```
vibcoding/
├── index.html         # HTML 结构
├── style.css          # 样式
├── data.json           # 所有数据（GitHub Actions 自动更新这个文件）
├── script.js           # 从 data.json 加载数据并渲染页面
│
├── .github/
│   └── workflows/
│       └── update-data.yml   # GitHub Actions 自动更新配置
│
└── update-data.js     # GitHub Actions 里运行的抓取脚本（用 GitHub Search API）
```

---

## 九、方案优势与弊端（给 v4pro 参考）

### ✅ 优势

| 维度 | 说明 |
|------|------|
| **一劳永逸** | GitHub Actions 每 2 天自动更新，用户打开网页就是最新的，无需手动操作 |
| **完全免费** | GitHub 仓库 + Pages + Actions 都是免费的，零成本运行 |
| **轻量无负担** | 纯前端页面，不需要服务器、数据库、域名 |
| **工作流清晰** | 需求文档写好 → 一次性交付 → 完事，后续加功能再迭代 |

### ❌ 弊端 & 应对建议

| 弊端 | 说明 | 应对建议 |
|------|------|---------|
| **Viral 案例无法自动更新** | 没有标准化 API，靠手动搜集 | 初始一次性填充，后续如需更新让用户找 AI 重新搜 |
| **GitHub Search API 有频率限制** | 免费账号每小时 10 次请求，搜"vibe coding"不一定每次都有新项目 | update-data.js 做好去重逻辑，避免重复数据 |
| **纯静态无互动** | 不能收藏、评论、标记已看 | 当前版本定位就是纯浏览，以后可迭代 |
| **依赖 GitHub 可用性** | GitHub 偶尔被墙，Actions 偶尔排队 | 用户在国内，注意资源引用使用国内可访问的 CDN |
| **自动数据质量不可控** | 搜到的项目不一定有"灵感点"，可能混入无关仓库 | update-data.js 用关键词 + star 数双重过滤，低质量自动跳过 |

---

## 十、交付物

- 完整的页面代码（所有文件）
- GitHub Actions 自动更新配置
- 推送到 GitHub + 启用 GitHub Pages
- 直接在 GitHub Pages 打开就能看
- 初始数据已填充完毕（至少 6~8 个 GitHub 项目 + 4~6 个 Viral 案例）
- 之后每 2 天自动更新 GitHub 项目数据
